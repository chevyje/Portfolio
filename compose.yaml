services:
  front-end:
    container_name: frontend_portfolio
    build:
      context: ./portfolio
      target: production
    expose:
      - "80"
    networks:
      - webnet

networks:
  webnet:
    external: true