services:
  front-end:
    container_name: frontend_portfolio
    build:
      context: ./portfolio
      target: production
    ports:
      - "10000:80"
    networks:
      - proxyNet

networks:
  proxyNet:
    external: true